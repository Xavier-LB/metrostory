{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://metrostory.cl/schemas/source.json",
	"title": "MetroStory Source",
	"description": "Una referencia bibliográfica o fuente de información",
	"type": "array",
	"items": {
		"type": "object",
		"required": ["id", "title"],
		"properties": {
			"id": {
				"type": "string",
				"pattern": "^[a-z0-9-]+$",
				"description": "Identificador único para referenciar esta fuente"
			},
			"title": {
				"type": "string",
				"minLength": 1,
				"description": "Título del artículo, libro o recurso"
			},
			"url": {
				"type": "string",
				"format": "uri",
				"description": "URL del recurso (si está disponible online)"
			},
			"consultedAt": {
				"type": "string",
				"pattern": "^[0-9]{4}(-[0-9]{2})?$",
				"description": "Fecha de consulta (YYYY o YYYY-MM)"
			}
		}
	}
}
